name: conda_env3.10
channels:
  - nvidia
  - conda-forge
  - defaults

dependencies:
  - python=3.10
  # CUDA / cuDNN — TF 2.11 officially requires CUDA 11.2 + cuDNN 8.1
  - cudatoolkit=11.8
  - cudnn=8.1.0.77
  # Everything else is installed via pip (pyproject.toml)
  - pip
  - pip:
      - -e .
