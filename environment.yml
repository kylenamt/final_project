name: conda_env3.10
channels:
  - nvidia
  - conda-forge
  - defaults

dependencies:
  - python=3.10
  # CUDA / cuDNN — TF 2.11 needs CUDA 11 + cuDNN 8
  - cudatoolkit=11.8
  - cudnn=8.9.2.26=cuda11_0
  # Everything else is installed via pip (pyproject.toml)
  - pip
  - pip:
      - -e .
