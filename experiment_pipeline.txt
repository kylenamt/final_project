Experiment description:
Dataset use: data/raw/voice/FULL/*

DDSP model:
run inference in for all data in Dataset
example snippet:
def run_synthesize_dir(model,dir,adjust feature settings)
    #apply the settings to each audio piece and run inference.
    #then save the audio to data/processed/voice/Full_transfered// can be set as input, or adapted from input dir

Baseline:
WORLD vocoder Baseline
Dataset use: data/raw/voice/FULL/*
source dataset: data/raw/solo_violin/*
preprocessing: remove all silent segment from source audio
source audio choice when run synthesizer: from the preprocessed audio, construct a "source bank" so the source segment are randomly select from the source.
then run processing the entire FULL dataset as well. using some sort of function:
def run_vocoder_dir: //you decide the parameters and name




